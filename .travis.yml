sudo: required
services:
    - docker

before_install:
    - docker build -t pavelsab/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true pavelsab/docker-react npm run test -- --coverage
